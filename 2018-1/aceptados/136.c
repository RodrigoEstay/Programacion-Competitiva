#include <stdio.h>
#include <stdlib.h>

/* La manera que se obtuvo este numero fue con otro programa que esta en la carpeta
"extras" con el mismo nombre que este, no se ocupo ese programa por que se demoraba
mas de 3 segundos. */

int main (){
	printf("The 1500'th ugly number is %d.\n",859963392);
	return 0;
}